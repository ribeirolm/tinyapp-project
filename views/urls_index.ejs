<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>TinyApp</title>
</head>
<body style="font-size:20px; padding:50px;">
    <!-- Add EJS code here -->
    <%include ./partials/_header %>
   <a href= '/urls/new'> Create tiny URL </a>
    <ul>
      <% if (user){%>
        <% for(var shortURL in userUrls){ %>
        <li> <a href='<%= userUrls[shortURL]%>'><%=userUrls[shortURL]%> - <%=userUrls[shortURL].longURL%> </a> </li>
        <form method="POST" action="/urls/<%=shortURL%>/delete">
          <a href= '/urls/<%= shortURL%>'> Edit </a>
          <button type="submit" class="btn btn-primary"> Delete </button>
        </form>
            <%}%>
      <%} else{%>
        <p>You must login or register to see URLS</p>
      <%};%>
    </ul>

</body>
</html>
